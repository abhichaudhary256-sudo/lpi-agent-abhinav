# lpi-agent-abhinav
Built an adaptive AI agent using the LPI MCP server that dynamically selects and queries multiple tools, combines outputs, and generates explainable answers using a local LLM (Ollama).
